import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";
import CodeBlock from "@theme/CodeBlock";
import Version from "@site/src/components/CliVersion";
import Admonition from "@theme/Admonition";

<Version />

<Tabs groupId="os-preference" className="api-tabs">

<TabItem value="macOS-or-linux" label="macOS and Linux">

Simply run the installer script in your terminal:

<CodeBlock language="bash">
  {`curl -L https://graphql-engine-cdn.hasura.io/ddn/cli/${props.revision || "v4"}/get.sh | bash`}
</CodeBlock>

</TabItem>

<TabItem value="windows" label="Windows">
- Download the latest <a href={`https://graphql-engine-cdn.hasura.io/ddn/cli/${props.revision || "v4"}/latest/DDN_CLI_Setup.exe`}>DDN CLI installer</a> for Windows.
- Once it is downloaded, run the installer (DDN_CLI_Setup.exe). This will only take a minute.
- By default, DDN CLI is installed under `C:\Users\{Username}\AppData\Local\Programs\DDN_CLI`.
<br />

<Admonition type="tip" icon="💡" title="Adding to %PATH%">
  Setup will add DDN CLI to your `%PATH%` by default, so you can type `ddn` from your terminal to access the CLI.
</Admonition>

</TabItem>
</Tabs>
