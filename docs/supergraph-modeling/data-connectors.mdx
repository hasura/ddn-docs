---
sidebar_position: 3
sidebar_label: Data Connectors
description:
  "Docs for the configuration of data connectors in Hasura. Learn how to connect to SQL & NoSQL databases, REST APIs,
  GraphQL API and more by specifying a connector URL and schema. Increase flexibility and efficiency by defining schema
  types, leveraging atomic builds, and implementing structured data model configuration."
keywords:
  - hasura opendd
  - data connector url
  - data connector headers
  - data connector schema
  - graphql api configuration
  - rest api configuration
  - database connectivity
  - nosql databases
  - sql databases
toc_max_heading_level: 4
seoFrontMatterUpdated: true
---

# Data Connectors

## Introduction

A data connector is a way to specify where your data comes from and how it can be used. It can connect to various types
of data sources, like SQL databases, NoSQL databases, REST APIs, GraphQL APIs, files, and more.

You can declare a data connector and use it in your [model's](/supergraph-modeling/models.mdx) configuration by
[referencing it with the `source`](/supergraph-modeling/models.mdx#source) property, which connects that model to the
respective data connector.

## Metadata structure

### DataConnectorLink

Definition of a data connector, used to bring in sources of data and connect them to OpenDD models and commands.

| Name | Type | Required | Description |
| ---- | ---- | -------- | ----------- |
| `name` | `string` | `true` | The name of the data connector. |
| `url` | [`DataConnectorUrlV1`](#dataconnectorurlv1) | `true` | The url(s) to access the data connector. |
| `headers` | [`EnvironmentValue`](#environmentvalue) | `false` | Key value map of HTTP headers to be sent with each request to the data connector. |
| `schema` | [`VersionedSchemaAndCapabilities`](#versionedschemaandcapabilities) | `true` | The schema of the data connector. This schema is used as the source of truth when serving requests and the live schema of the data connector is not looked up. |



**Example:**

```yaml
name: data_connector
url:
  singleUrl:
    value: http://data_connector:8100
headers: {}
schema:
  version: v0.1
  schema:
    scalar_types: {}
    object_types: {}
    collections: []
    functions: []
    procedures: []
  capabilities:
    version: 0.1.0
    capabilities:
      query:
        variables: {}
      mutation: {}

```

| Name | Type | Required | Description |
| ---- | ---- | -------- | ----------- |
| `singleUrl` |  | `true` | Either a literal string or a reference to a Hasura secret |

#### EnvironmentValue

Either a literal string or a reference to a Hasura secret

| Name | Type | Required | Description |
| ---- | ---- | -------- | ----------- |
| `value` | `string` | `true` |  |

| Name | Type | Required | Description |
| ---- | ---- | -------- | ----------- |
| `valueFromEnv` | `string` | `true` |  |

| Name | Type | Required | Description |
| ---- | ---- | -------- | ----------- |
| `readWriteUrls` | `object` | `true` |  |

#### ReadWriteUrls

| Name | Type | Required | Description |
| ---- | ---- | -------- | ----------- |
| `read` |  | `true` | Either a literal string or a reference to a Hasura secret |
| `write` |  | `true` | Either a literal string or a reference to a Hasura secret |

#### VersionedSchemaAndCapabilities

#### SchemaAndCapabilitiesV01

| Name | Type | Required | Description |
| ---- | ---- | -------- | ----------- |
| `version` | `string` | `true` |  |
| `schema` | `object` | `true` |  |
| `capabilities` | `object` | `true` |  |

#### Schema Response

| Name | Type | Required | Description |
| ---- | ---- | -------- | ----------- |
| `scalar_types` | [`Scalar Type`](#scalar type) | `true` | A list of scalar types which will be used as the types of collection columns |
| `object_types` | [`Object Type`](#object type) | `true` | A list of object types which can be used as the types of arguments, or return types of procedures. Names should not overlap with scalar type names. |
| `collections` | [`Collection Info`](#collection info) | `true` | Collections which are available for queries |
| `functions` | [`Function Info`](#function info) | `true` | Functions (i.e. collections which return a single column and row) |
| `procedures` | [`Procedure Info`](#procedure info) | `true` | Procedures which are available for execution as part of mutations |

#### Collection Info

| Name | Type | Required | Description |
| ---- | ---- | -------- | ----------- |
| `name` | `string` | `true` | The name of the collection  Note: these names are abstract - there is no requirement that this name correspond to the name of an actual collection in the database. |
| `description` | `string,null` | `false` | Description of the collection |
| `arguments` | [`Argument Info`](#argument info) | `true` | Any arguments that this collection requires |
| `type` | `string` | `true` | The name of the collection's object type |
| `uniqueness_constraints` | [`Uniqueness Constraint`](#uniqueness constraint) | `true` | Any uniqueness constraints enforced on this collection |
| `foreign_keys` | [`Foreign Key Constraint`](#foreign key constraint) | `true` | Any foreign key constraints enforced on this collection |

#### Function Info

| Name | Type | Required | Description |
| ---- | ---- | -------- | ----------- |
| `name` | `string` | `true` | The name of the function |
| `description` | `string,null` | `false` | Description of the function |
| `arguments` | [`Argument Info`](#argument info) | `true` | Any arguments that this collection requires |
| `result_type` | `undefined` | `true` | The name of the function's result type |

#### Type

Types track the valid representations of values as JSON

| Name | Type | Required | Description |
| ---- | ---- | -------- | ----------- |
| `type` | `string` | `true` |  |
| `name` | `string` | `true` | The name can refer to a primitive type or a scalar type |

| Name | Type | Required | Description |
| ---- | ---- | -------- | ----------- |
| `type` | `string` | `true` |  |
| `underlying_type` | `undefined` | `true` | The type of the non-null inhabitants of this type |

| Name | Type | Required | Description |
| ---- | ---- | -------- | ----------- |
| `type` | `string` | `true` |  |
| `element_type` | `undefined` | `true` | The type of the elements of the array |

| Name | Type | Required | Description |
| ---- | ---- | -------- | ----------- |
| `type` | `string` | `true` |  |
| `object_type_name` | `string` | `true` | The object type name |

#### Procedure Info

| Name | Type | Required | Description |
| ---- | ---- | -------- | ----------- |
| `name` | `string` | `true` | The name of the procedure |
| `description` | `string,null` | `false` | Column description |
| `arguments` | [`Argument Info`](#argument info) | `true` | Any arguments that this collection requires |
| `result_type` | `undefined` | `true` | The name of the result type |

#### Capabilities Response

| Name | Type | Required | Description |
| ---- | ---- | -------- | ----------- |
| `version` | `string` | `true` |  |
| `capabilities` | `object` | `true` | Describes the features of the specification which a data connector implements. |

#### Capabilities

Describes the features of the specification which a data connector implements.

| Name | Type | Required | Description |
| ---- | ---- | -------- | ----------- |
| `query` | `object` | `true` |  |
| `mutation` | `object` | `true` |  |
| `relationships` | [`Relationship Capabilities`](#relationship capabilities) | `false` |  |

#### Query Capabilities

| Name | Type | Required | Description |
| ---- | ---- | -------- | ----------- |
| `aggregates` | `object` | `false` | Does the connector support aggregate queries |
| `variables` | `object` | `false` | Does the connector support queries which use variables |
| `explain` | `object` | `false` | Does the connector support explaining queries |

#### Mutation Capabilities

| Name | Type | Required | Description |
| ---- | ---- | -------- | ----------- |
| `transactional` | `object` | `false` | Does the connector support executing multiple mutations in a transaction. |
| `explain` | `object` | `false` | Does the connector support explaining mutations |

#### Relationship Capabilities

| Name | Type | Required | Description |
| ---- | ---- | -------- | ----------- |
| `relation_comparisons` | `object` | `false` | Does the connector support comparisons that involve related collections (ie. joins)? |
| `order_by_aggregate` | `object` | `false` | Does the connector support ordering by an aggregated array relationship? |

### DataConnectorScalarRepresentation

The representation of a data connector scalar in terms of Open DD types

| Name | Type | Required | Description |
| ---- | ---- | -------- | ----------- |
| `dataConnectorName` | `string` | `true` | The name of the data connector that this scalar type comes from. |
| `dataConnectorScalarType` | `string` | `true` | The name of the scalar type coming from the data connector. |
| `representation` | `undefined` | `true` | The name of the Open DD type that this data connector scalar type should be represented as. |
| `graphql` | [`DataConnectorScalarGraphQLConfiguration`](#dataconnectorscalargraphqlconfiguration) | `false` | Configuration for how this scalar's operators should appear in the GraphQL schema. |



**Example:**

```yaml
dataConnectorName: data_connector
dataConnectorScalarType: varchar
representation: String
graphql:
  comparisonExpressionTypeName: String_Comparison_Exp

```

#### DataConnectorScalarGraphQLConfiguration

GraphQL configuration of a data connector scalar

| Name | Type | Required | Description |
| ---- | ---- | -------- | ----------- |
| `comparisonExpressionTypeName` | `string` | `false` |  |

