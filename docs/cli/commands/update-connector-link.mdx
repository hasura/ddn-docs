---
title: hasura3 update connector-link
sidebar_position: 43
sidebar_label: ddn update connector-link
description: Using the ddn update connector-link command with the Hasura CLI
---

# Hasura DDN CLI: ddn update connector-link 

## Synopsis
Updates the NDC schema of the connector link.

```bash
$ ddn update connector-link --help
Updates the NDC schema of the connector link

Usage:
  ddn update connector-link [connector name] [flags]

Aliases:
  connector-link, cl, Connectorlink, connectorlink

Examples:
 # Update DataConnectorLink using the running connector URL specified in the ConnectorLink File
   ddn update connector-link <connector-name> --subgraph app

 # Update DataConnectorLink using the Deployment ID returned by the 'ddn build connectormanifest' command
   ddn update connector-link <connector-name> --subgraph app --deployment-id <deployment-id>

 # Update DataConnectorLink using the running connector URL referenced using an env var CONNECTOR_URL
   ddn update connector-link <connector-name> --subgraph app -e CONNECTOR_URL=https://connector-host:post


Flags:
  -i, --deployment-id uuid   The Build ID (applicable only to Cloud connector deployments) 
  -e, --env strings          env vars
  -h, --help                 help for connector-link
      --subgraph string      Subgraph name

Global Flags:
      --dir string                   DDN project directory in which hasura.yaml is present (default ".")
      --log-level string             Log level. accepts DEBUG, WARN, INFO, ERROR, FATAL (default "INFO")
      --no-prompt                    Set this flag to no prompt for anything, the command fails if any input is missing
  -o, --out string                   Format in which the output should be printed, accepts table, json and yaml (default "table")
  -p, --project string               DDN Project Name
  -s, --supergraph-manifest string   Supergraph manifest to use. (Optional. DefaultSupergraphManifest used  if not provided)
```

