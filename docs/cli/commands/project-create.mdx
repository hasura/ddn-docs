---
title: hasura3 project create
sidebar_position: 33
sidebar_label: hasura3 project create
description: Using the hasura3 project create command with the Hasura CLI
---

# Hasura3 CLI: hasura3 project create 

## Synopsis
Create a new project in Hasura Cloud.

```bash
$ hasura3 project create --help
Create a new project in Hasura Cloud

Usage:
  hasura3 project create [flags]

Examples:
 # Create a project
   hasura3 project create

Flags:
  -h, --help   help for create

Global Flags:
      --dir string         Hasura project directory in which hasura.yaml is present (default ".")
      --log-level string   Log level. accepts DEBUG, WARN, INFO, ERROR, FATAL (default "INFO")
      --no-prompt          set this flag to no prompt for anything, the command fails if any input is missing
  -o, --out string         format in which the output should be printed, accepts table, json and yaml (default "table")
  -p, --project string     Hasura DDN Project Name
```

