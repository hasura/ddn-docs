---
title: hasura3 namespace describe
sidebar_position: 27
sidebar_label: hasura3 namespace describe
description: Using the hasura3 namespace describe command with the Hasura CLI
---

# Hasura3 CLI: hasura3 namespace describe 

## Synopsis
View namespace details [aliases: details, info].

```bash
$ hasura3 namespace describe --help
View namespace details [aliases: details, info]

Usage:
  hasura3 namespace describe [flags]

Aliases:
  describe, details, info

Examples:
 # View details of a "accounting" namespace
   hasura3 namespace describe --project <project-name> --name accounting

Flags:
  -h, --help          help for describe
  -n, --name string   Namespace name (required)

Global Flags:
      --dir string         Hasura project directory in which hasura.yaml is present (default ".")
      --log-level string   Log level. accepts DEBUG, WARN, INFO, ERROR, FATAL (default "INFO")
      --no-prompt          set this flag to no prompt for anything, the command fails if any input is missing
  -o, --out string         format in which the output should be printed, accepts table, json and yaml (default "table")
  -p, --project string     Hasura DDN Project Name
```

