---
title: hasura3 namespace create
sidebar_position: 25
sidebar_label: hasura3 namespace create
description: Using the hasura3 namespace create command with the Hasura CLI
---

# Hasura3 CLI: hasura3 namespace create 

## Synopsis
Create a namespace.

```bash
$ hasura3 namespace create --help
Create a namespace

Usage:
  hasura3 namespace create [flags]

Examples:
 # Create a Namespace "accounting" in a project
   hasura3 namespace create --name accounting --project <project-name>

Flags:
  -h, --help          help for create
  -n, --name string   Namespace name (required)

Global Flags:
      --dir string         Hasura project directory in which hasura.yaml is present (default ".")
      --log-level string   Log level. accepts DEBUG, WARN, INFO, ERROR, FATAL (default "INFO")
      --no-prompt          set this flag to no prompt for anything, the command fails if any input is missing
  -o, --out string         format in which the output should be printed, accepts table, json and yaml (default "table")
  -p, --project string     Hasura DDN Project Name
```

