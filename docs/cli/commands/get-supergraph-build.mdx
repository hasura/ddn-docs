---
title: hasura3 get supergraph-build
sidebar_position: 31
sidebar_label: ddn get supergraph-build
description: Using the ddn get supergraph-build command with the Hasura CLI
---

# Hasura DDN CLI: ddn get supergraph-build 

## Synopsis
See docs here: https://hasura.io/docs/3.0/ci-cd/builds/.

```bash
$ ddn get supergraph-build --help
See docs here: https://hasura.io/docs/3.0/ci-cd/builds/

Usage:
  ddn get supergraph-build [flags]

Aliases:
  supergraph-build, SupergraphBuild, spgb, sb

Examples:
 # View details of a SupergraphBuild in your project
   ddn get supergraph-build <build-version> --project <project-name>

 # List all SupergraphBuilds (across all environments) of a project
   ddn get supergraph-build --project <project-name>

 # List all SupergraphBuilds in an environment of a project
   ddn get supergraph-build --project <project-name> --environment <environment-name>


Flags:
      --environment string   environment of the builds
  -h, --help                 help for supergraph-build

Global Flags:
      --dir string                   DDN project directory in which hasura.yaml is present (default ".")
      --log-level string             Log level. accepts DEBUG, WARN, INFO, ERROR, FATAL (default "INFO")
      --no-prompt                    Set this flag to no prompt for anything, the command fails if any input is missing
  -o, --out string                   Format in which the output should be printed, accepts table, json and yaml (default "table")
  -p, --project string               DDN Project Name
  -s, --supergraph-manifest string   Supergraph manifest to use. (Optional. DefaultSupergraphManifest used  if not provided)
```

