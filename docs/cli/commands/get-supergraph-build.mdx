---
title: hasura3 get supergraph-build
sidebar_position: 29
sidebar_label: ddn get supergraph-build
description: Using the ddn get supergraph-build command with the Hasura CLI
---

# Hasura DDN CLI: ddn get supergraph-build 

## Synopsis
See docs here: https://hasura.io/docs/3.0/project-configuration/builds/.

```bash
$ ddn get supergraph-build --help
See docs here: https://hasura.io/docs/3.0/project-configuration/builds/

Usage:
  ddn get supergraph-build [flags]

Aliases:
  supergraph-build, SupergraphBuild, spgb, sb

Examples:
 # View details of a SupergraphBuild in a Project
   ddn get supergraph-build <build-version>

 # List all SupergraphBuilds of a Project
   ddn get supergraph-build


Flags:
  -h, --help   help for supergraph-build

Global Flags:
      --dir string                   DDN Project directory in which hasura.yaml is present (default ".")
      --log-level string             Log level. Can be DEBUG, WARN, INFO, ERROR, or FATAL. (default "INFO")
      --no-prompt                    Do not prompt for required but missing flags
  -o, --out string                   Output format. Can be table, json or yaml. (default "table")
  -p, --project string               DDN Project name
  -s, --supergraph-manifest string   SupergraphManifest to use.
```

