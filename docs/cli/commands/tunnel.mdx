---
title: hasura3 tunnel
sidebar_position: 40
sidebar_label: ddn tunnel
description: Using the ddn tunnel command with the Hasura CLI
---

# Hasura DDN CLI: ddn tunnel 

## Synopsis
See docs here: https://hasura.io/docs/3.0/ci-cd/tunnels/.

```bash
$ ddn tunnel --help
See docs here: https://hasura.io/docs/3.0/ci-cd/tunnels/

Usage:
  ddn tunnel [command]

Available Commands:
  start       Start DDN Secure Connect tunnel

Flags:
  -h, --help   help for tunnel

Global Flags:
      --dir string                   DDN project directory in which hasura.yaml is present (default ".")
      --log-level string             Log level. accepts DEBUG, WARN, INFO, ERROR, FATAL (default "INFO")
      --no-prompt                    Set this flag to no prompt for anything, the command fails if any input is missing
  -o, --out string                   Format in which the output should be printed, accepts table, json and yaml (default "table")
  -p, --project string               DDN Project Name
  -s, --supergraph-manifest string   Supergraph manifest to use. (Optional. DefaultSupergraphManifest used  if not provided)

Use "ddn tunnel [command] --help" for more information about a command.
```

