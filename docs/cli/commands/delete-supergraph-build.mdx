---
title: hasura3 delete supergraph-build
sidebar_position: 22
sidebar_label: ddn delete supergraph-build
description: Using the ddn delete supergraph-build command with the Hasura CLI
---

# Hasura DDN CLI: ddn delete supergraph-build 

## Synopsis
See docs here: https://hasura.io/docs/3.0/ci-cd/builds/.

```bash
$ ddn delete supergraph-build --help
See docs here: https://hasura.io/docs/3.0/ci-cd/builds/

Usage:
  ddn delete supergraph-build [flags]

Aliases:
  supergraph-build, SupergraphBuild, spgb, sb

Examples:
 # Delete a SupergraphBuild from a Project
   ddn delete supergraph-build  <build-version>


Flags:
  -h, --help   help for supergraph-build

Global Flags:
      --dir string                   DDN Project directory in which hasura.yaml is present (default ".")
      --log-level string             Log level. Can be DEBUG, WARN, INFO, ERROR, or FATAL. (default "INFO")
      --no-prompt                    Do not prompt for required but missing flags
  -o, --out string                   Output format. Can be table, json or yaml. (default "table")
  -p, --project string               DDN Project name
  -s, --supergraph-manifest string   SupergraphManifest to use.
```

