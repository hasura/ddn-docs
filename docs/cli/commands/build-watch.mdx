---
title: hasura3 build watch
sidebar_position: 8
sidebar_label: hasura3 build watch
description: Using the hasura3 build watch command with the Hasura CLI
---

# Hasura3 CLI: hasura3 build watch 

## Synopsis
Watch a local Hasura project.

```bash
$ hasura3 build watch --help
Watch a local Hasura project

Usage:
  hasura3 build watch [flags]

Examples:
 # Watch and re-create a build on any file changes 
   hasura3 build watch --dir <path-to-hasura.yaml-file>  

 # To override the default build profile, provide the --profile flag with a build-profile
   hasura3 build watch --profile build-profile-staging.yaml --dir <path-to-hasura.yaml-file>


Flags:
      --config string    (Optional) config file to watch connectors config (default "connectors.yaml")
  -h, --help             help for watch
      --profile string   Build profile to use, uses the default build profile if not provided

Global Flags:
      --dir string         Hasura project directory in which hasura.yaml is present (default ".")
      --log-level string   Log level. accepts DEBUG, WARN, INFO, ERROR, FATAL (default "INFO")
      --no-prompt          set this flag to no prompt for anything, the command fails if any input is missing
  -o, --out string         format in which the output should be printed, accepts table, json and yaml (default "table")
  -p, --project string     Hasura DDN Project Name
```

