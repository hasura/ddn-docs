---
title: hasura3 create
sidebar_position: 16
sidebar_label: ddn create
description: Using the ddn create command with the Hasura CLI
---

# Hasura DDN CLI: ddn create 

## Synopsis
Create a new resource on Hasura DDN.

```bash
$ ddn create --help
Create a new resource on Hasura DDN

Usage:
  ddn create [command]

Examples:
 # Create a new Project
   ddn create project

 # Create a new Subgraph in a Project
   ddn create subgraph <subgraph-name>


Available Commands:
  project     Create a new Project on Hasura DDN
  subgraph    Create a new Subgraph in a Project

Flags:
  -h, --help   help for create

Global Flags:
      --dir string                   DDN Project directory in which hasura.yaml is present (default ".")
      --log-level string             Log level. Can be DEBUG, WARN, INFO, ERROR, or FATAL. (default "INFO")
      --no-prompt                    Do not prompt for required but missing flags
  -o, --out string                   Output format. Can be table, json or yaml. (default "table")
  -p, --project string               DDN Project name
  -s, --supergraph-manifest string   SupergraphManifest to use.

Use "ddn create [command] --help" for more information about a command.
```

