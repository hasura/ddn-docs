---
title: hasura3 add command
sidebar_position: 3
sidebar_label: ddn add command
description: Using the ddn add command command with the Hasura CLI
---

# Hasura DDN CLI: ddn add command 

## Synopsis
Add a new Command to the local metadata.

```bash
$ ddn add command --help
Add a new Command to the local metadata

Usage:
  ddn add command [flags]

Examples:
 # Add a new Command 'Login' from the procedure 'Login' in the DataConnectorLink 'actions'
   ddn add command --data-connector-link actions --name Login
	 

Flags:
      --data-connector-link string   name of the data connector link
  -h, --help                         help for command
      --name string                  name of the collection to add as a model to the metadata
      --subgraph string              name of the Subgraph to add the connector to

Global Flags:
      --dir string                   DDN project directory in which hasura.yaml is present (default ".")
      --log-level string             Log level. accepts DEBUG, WARN, INFO, ERROR, FATAL (default "INFO")
      --no-prompt                    Set this flag to no prompt for anything, the command fails if any input is missing
  -o, --out string                   Format in which the output should be printed, accepts table, json and yaml (default "table")
  -p, --project string               DDN Project Name
  -s, --supergraph-manifest string   SupergraphManifest to use. (Optional. DefaultSupergraphManifest used  if not provided)
```

