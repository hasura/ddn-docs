---
title: hasura3 namespace delete
sidebar_position: 26
sidebar_label: hasura3 namespace delete
description: Using the hasura3 namespace delete command with the Hasura CLI
---

# Hasura3 CLI: hasura3 namespace delete 

## Synopsis
Remove a namespace from Hasura Cloud Project [aliases: remove, rm].

```bash
$ hasura3 namespace delete --help
Remove a namespace from Hasura Cloud Project [aliases: remove, rm]

Usage:
  hasura3 namespace delete [flags]

Aliases:
  delete, rm, remove

Examples:
 # Delete the namespace "accounting"
   hasura3 namespace delete --project <project-name> --name accounting

Flags:
  -h, --help          help for delete
  -n, --name string   Namespace to be deleted (required)

Global Flags:
      --dir string         Hasura project directory in which hasura.yaml is present (default ".")
      --log-level string   Log level. accepts DEBUG, WARN, INFO, ERROR, FATAL (default "INFO")
      --no-prompt          set this flag to no prompt for anything, the command fails if any input is missing
  -o, --out string         format in which the output should be printed, accepts table, json and yaml (default "table")
  -p, --project string     Hasura DDN Project Name
```

