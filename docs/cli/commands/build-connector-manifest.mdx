---
title: "ddn build connector-manifest"
sidebar_position: 8
sidebar_label: "ddn build connector-manifest"
description: "Using the ddn build connector-manifest command with the Hasura CLI"
---

# Hasura DDN CLI: ddn build connector-manifest

## Synopsis
Build and deploy a data connector using its ConnectorManifest

```bash
$ ddn build connector-manifest --help
Build and deploy a data connector using its ConnectorManifest

Usage:
  ddn build connector-manifest <name> [flags]

Aliases:
  connector-manifest, ConnectorManifest, cm

Examples:
 # Create ConnectorBuild "mydb" on Subgraph "myapp"
  ddn build connector-manifest mydb --subgraph myapp


Flags:
  -h, --help              help for connector-manifest
  -s, --subgraph string   Name of the Subgraph

Global Flags:
      --dir string                   DDN Project directory in which hasura.yaml is present (default ".")
      --log-level string             Log level. Can be DEBUG, WARN, INFO, ERROR, or FATAL. (default "INFO")
      --no-prompt                    Do not prompt for required but missing flags
      --out string                   Output format. Can be table, json or yaml. (default "table")
  -p, --project string               DDN Project name
      --supergraph-manifest string   SupergraphManifest to use
```

