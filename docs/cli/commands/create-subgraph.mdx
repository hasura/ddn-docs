---
title: hasura3 create subgraph
sidebar_position: 18
sidebar_label: ddn create subgraph
description: Using the ddn create subgraph command with the Hasura CLI
---

# Hasura DDN CLI: ddn create subgraph 

## Synopsis
See docs here: https://hasura.io/docs/3.0/ci-cd/subgraphs/.

```bash
$ ddn create subgraph --help
See docs here: https://hasura.io/docs/3.0/ci-cd/subgraphs/

Usage:
  ddn create subgraph <name> [flags]

Aliases:
  subgraph, Subgraph, s

Examples:
 # Create a new Subgraph 'app' in a Project
   ddn create subgraph app


Flags:
  -h, --help   help for subgraph

Global Flags:
      --dir string                   DDN Project directory in which hasura.yaml is present (default ".")
      --log-level string             Log level. Can be DEBUG, WARN, INFO, ERROR, or FATAL. (default "INFO")
      --no-prompt                    Do not prompt for required but missing flags
      --out string                   Output format. Can be table, json or yaml. (default "table")
  -p, --project string               DDN Project name
      --supergraph-manifest string   SupergraphManifest to use
```

