---
title: hasura3 init
sidebar_position: 21
sidebar_label: hasura3 init
description: Using the hasura3 init command with the Hasura CLI
---

# Hasura3 CLI: hasura3 init 

## Synopsis
Init Hasura DDN project.

```bash
$ hasura3 init --help
Init Hasura DDN project

Usage:
  hasura3 init [flags]

Flags:
  -d, --dir string   Directory where the project should be created (default "/Users/sean/ProjectsHasura/v3-docs/utilities/auto-cli")
  -h, --help         help for init

Global Flags:
      --log-level string   Log level. accepts DEBUG, WARN, INFO, ERROR, FATAL (default "INFO")
      --no-prompt          set this flag to no prompt for anything, the command fails if any input is missing
  -o, --out string         format in which the output should be printed, accepts table, json and yaml (default "table")
  -p, --project string     Hasura DDN Project Name
```

