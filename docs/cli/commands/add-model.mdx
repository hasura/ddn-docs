---
title: "ddn add model"
sidebar_position: 4
sidebar_label: "ddn add model"
description: "Using the ddn add model command with the Hasura CLI"
---

# Hasura DDN CLI: ddn add model

## Synopsis
Add a new Model to the local metadata

```bash
$ ddn add model --help
Add a new Model to the local metadata

Usage:
  ddn add model [flags]

Examples:
 # Add a new Model "Album" from the collection "Album" in the DataConnectorLink "mypg"
   ddn add model --data-connector-link mypg --name Album
	 

Flags:
      --data-connector-link string   Name of the DataConnectorLink
  -h, --help                         help for model
      --name string                  Name of the collection to add as Model
      --subgraph string              Subgraph that contains the DataConnectorLink

Global Flags:
      --dir string                   DDN Project directory in which hasura.yaml is present (default ".")
      --log-level string             Log level. Can be DEBUG, WARN, INFO, ERROR, or FATAL. (default "INFO")
      --no-prompt                    Do not prompt for required but missing flags
      --out string                   Output format. Can be table, json or yaml. (default "table")
  -p, --project string               DDN Project name
      --supergraph-manifest string   SupergraphManifest to use
```

