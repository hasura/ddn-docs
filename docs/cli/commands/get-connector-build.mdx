---
title: hasura3 get connector-build
sidebar_position: 25
sidebar_label: ddn get connector-build
description: Using the ddn get connector-build command with the Hasura CLI
---

# Hasura DDN CLI: ddn get connector-build 

## Synopsis
List Connector Builds or get details of a specific one.

```bash
$ ddn get connector-build --help
List Connector Builds or get details of a specific one

Usage:
  ddn get connector-build [flags]

Aliases:
  connector-build, cb, connectorbuild

Examples:
 # View details of a ConnectorBuild in a Project
   ddn get connector-build <connector-name>

 # List all ConnectorBuilds of a Project
   ddn get connector-build


Flags:
      --build-id uuid     Connector Build Id
  -h, --help              help for connector-build
      --schema            Show NDC schema
      --subgraph string   Subgraph Name

Global Flags:
      --dir string                   DDN Project directory in which hasura.yaml is present (default ".")
      --log-level string             Log level. Can be DEBUG, WARN, INFO, ERROR, or FATAL. (default "INFO")
      --no-prompt                    Do not prompt for required but missing flags
  -o, --out string                   Output format. Can be table, json or yaml. (default "table")
  -p, --project string               DDN Project name
  -s, --supergraph-manifest string   SupergraphManifest to use.
```

