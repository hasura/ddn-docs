---
title: hasura3 secret get
sidebar_position: 41
sidebar_label: hasura3 secret get
description: Using the hasura3 secret get command with the Hasura CLI
---

# Hasura3 CLI: hasura3 secret get 

## Synopsis
Get the value of a secret key.

```bash
$ hasura3 secret get --help
Get the value of a secret key

Usage:
  hasura3 secret get [flags]

Examples:
 # Get the value of secret MY_KEY
   hasura3 secret get --project <project-name> --key MY_KEY

 # Get the value of secret MY_KEY in default environment (across all namespaces) of a project
   hasura3 secret get --project <project-name> --key MY_KEY --environment default

 # Get the value of secret MY_KEY in default namespace (across all environments) of a project
   hasura3 secret get --project <project-name> --namespace default -key MY_KEY

 # Get the value of secret MY_KEY in default environment in default namespace
   hasura3 secret get --project <project-name> --namespace default --environment default -key MY_KEY

Flags:
  -e, --environment string   Environment of secret
  -h, --help                 help for get
  -k, --key string           SetSecret key (required)
  -n, --namespace string     Namespace of secret

Global Flags:
      --dir string         Hasura project directory in which hasura.yaml is present (default ".")
      --log-level string   Log level. accepts DEBUG, WARN, INFO, ERROR, FATAL (default "INFO")
      --no-prompt          set this flag to no prompt for anything, the command fails if any input is missing
  -o, --out string         format in which the output should be printed, accepts table, json and yaml (default "table")
  -p, --project string     Hasura DDN Project Name
```

