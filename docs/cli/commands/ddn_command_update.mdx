---
sidebar_label: ddn command update
sidebar_position: 10
description: Update a Command in the local metadata using the DDN CLI
keywords:
  - hasura
  - DDN
  - docs
  - CLI
  - ddn command update
---

# DDN CLI: ddn command update

Update a Command in the local metadata.

## Synopsis

Update a Command in the local metadata

```bash
ddn command update <command-name> [flags]
```

## Examples

```bash
# Update the Command "Login" in "app" Subgraph config
 ddn command update Login --subgraph ./app/subgraph.yaml
```

## Options

```sass
-h, --help              help for update
    --subgraph string   Path to Subgraph config file
```

## Options inherited from parent operations

```sass
--log-level string   Log level. Can be DEBUG, WARN, INFO, ERROR, or FATAL. (default "INFO")
--no-prompt          Do not prompt for required but missing flags
--out string         Output format. Can be table, json or yaml. (default "table")
--timeout int        Request timeout in seconds [env: HASURA_DDN_TIMEOUT] (default 100)
```

## Parent operation

- [ddn command](/cli/commands/ddn_command) - Perform Command related operations
