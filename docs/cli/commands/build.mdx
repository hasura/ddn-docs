---
title: hasura3 build
sidebar_position: 8
sidebar_label: ddn build
description: Using the ddn build command with the Hasura CLI
---

# Hasura DDN CLI: ddn build 

## Synopsis
Build either a SupergraphManifest or a ConnectorManifest.

```bash
$ ddn build --help
Build either a SupergraphManifest or a ConnectorManifest

Usage:
  ddn build [command]

Examples:
 # Build a Data Connector
   ddn build connector-manifest <connector-name> --subgraph <subgraph-name> --wait

 # Create a SupergraphBuild using the default SupergraphManifest
   ddn build supergraph-manifest


Available Commands:
  connector-manifest  Build and deploy a data connector using its ConnectorManifest
  supergraph-manifest Create a new SupergraphBuild

Flags:
  -h, --help   help for build

Global Flags:
      --dir string                   DDN Project directory in which hasura.yaml is present (default ".")
      --log-level string             Log level. Can be DEBUG, WARN, INFO, ERROR, or FATAL. (default "INFO")
      --no-prompt                    Do not prompt for required but missing flags
  -o, --out string                   Output format. Can be table, json or yaml. (default "table")
  -p, --project string               DDN Project name
  -s, --supergraph-manifest string   SupergraphManifest to use.

Use "ddn build [command] --help" for more information about a command.
```

