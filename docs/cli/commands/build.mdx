---
title: hasura3 build
sidebar_position: 6
sidebar_label: ddn build
description: Using the ddn build command with the Hasura CLI
---

# Hasura DDN CLI: ddn build 

## Synopsis
Create Supergraph and Data Connector builds.

```bash
$ ddn build --help
Create Supergraph and Data Connector builds

Usage:
  ddn build [command]

Examples:
 # Build a Data Connector
   ddn build connector <connector-name> --subgraph <subgraph-name> --wait

 # Create a SupergraphBuild using the default SupergraphManifest
   ddn build supergraph   


Available Commands:
  connector   Build and deploy a data connector using its ConnectorManifest
  supergraph  Create a new SupergraphBuild

Flags:
  -h, --help   help for build

Global Flags:
      --dir string                   DDN project directory in which hasura.yaml is present (default ".")
      --log-level string             Log level. accepts DEBUG, WARN, INFO, ERROR, FATAL (default "INFO")
      --no-prompt                    Set this flag to no prompt for anything, the command fails if any input is missing
  -o, --out string                   Format in which the output should be printed, accepts table, json and yaml (default "table")
  -p, --project string               DDN Project Name
  -s, --supergraph-manifest string   SupergraphManifest to use. (Optional. DefaultSupergraphManifest used  if not provided)

Use "ddn build [command] --help" for more information about a command.
```

