---
title: hasura3 create project
sidebar_position: 16
sidebar_label: ddn create project
description: Using the ddn create project command with the Hasura CLI
---

# Hasura DDN CLI: ddn create project 

## Synopsis
See docs here: https://hasura.io/docs/3.0/ci-cd/projects/.

```bash
$ ddn create project --help
See docs here: https://hasura.io/docs/3.0/ci-cd/projects/

Usage:
  ddn create project [flags]

Aliases:
  project, Project

Examples:
 # Create a Project on Hasura DDN
   ddn create project
   
 # Create a Project on Hasura DDN and initialize a local project directory
   ddn create project --dir <project-dir>


Flags:
      --ddn-id uuid   The Data Delivery Network where the project should be hosted
  -d, --dir string    Directory where the project should be initialized
      --force         Initialize in a non-empty directory overwriting existing files if necessary
  -h, --help          help for project

Global Flags:
      --log-level string             Log level. accepts DEBUG, WARN, INFO, ERROR, FATAL (default "INFO")
      --no-prompt                    Set this flag to no prompt for anything, the command fails if any input is missing
  -o, --out string                   Format in which the output should be printed, accepts table, json and yaml (default "table")
  -p, --project string               DDN Project Name
  -s, --supergraph-manifest string   SupergraphManifest to use. (Optional. DefaultSupergraphManifest used  if not provided)
```

