---
title: hasura3 cloud environment remove
sidebar_position: 20
sidebar_label: hasura3 cloud environment remove
description: Using the hasura3 cloud environment remove command with the Hasura CLI
---

# Hasura3 CLI: hasura3 cloud environment remove 

## Synopsis
Remove an environment from Hasura Cloud Project.

```bash
$ hasura3 cloud environment remove --help
Remove an environment from Hasura Cloud Project

Usage: hasura3 cloud environment remove [OPTIONS] --name <env_name> <--project <project_config>|--pid <project_id>>

Options:
  -p, --project <project_config>  Path to your Project Config (hasura.yaml) file or a directory
                                  containing that file
      --pid <project_id>          Your Hasura Cloud Project ID
      --name <env_name>           Environment name
      --log-level <log-level>     The log level to be printed in stdout [env: HASURA_CLI_LOG_LEVEL=]
                                  [possible values: error, warn, info, debug, trace, error-all,
                                  warn-all, info-all, debug-all, trace-all]
  -h, --help                      Print help
```

